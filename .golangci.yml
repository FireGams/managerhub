run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gofmt

linters-settings:
  errcheck:
    exclude-functions:
      - (*database/sql.Rows).Close

issues:
  exclude-use-default: false
